<div class="row justify-content-center">
    <div class="col-4">
        <h1 class="text-center">WEBTEL</h1>
        <%= search_form_for(@q, class: "d-flex") do |f| %>
            <%= f.search_field :name_or_department_name_cont, 
                                placeholder: "Type Here to Search", 
                                class: "form-control me-2" %>
            <%= f.submit %>
        <% end %>
    </div>
</div>